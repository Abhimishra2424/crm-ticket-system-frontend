import { Button } from "react-bootstrap";

function App() {
  return (
    <div className="App">
      <Button variant="primary" onClick={() => alert("hello ")}>
        abhishek
      </Button>
    </div>
  );
}

export default App;
